# Checklist de tests manuels

Ce document liste ce qui doit être vérifié manuellement (ou via une app Laravel de test) avant de considérer les correctifs récents comme validés. La suite automatisée (`npm test`, 28 tests unitaires + 133 tests Storybook) couvre la logique pure ; elle ne couvre pas le rendu réel dans un navigateur, la soumission de formulaire native, ni l'intégration Blade.

## DataTable — mode `serverSide`

- [x] Brancher `PrDataTable` en `serverSide` sur un vrai endpoint Laravel `Model::paginate()` **forme wrappée en API Resource `{ data, meta }`** via `fromLaravelPaginator`. Validé le 2026-09-09 dans `forge` (`/users`, 51 lignes réelles, pagination sur 6 pages). — La **forme brute** (non wrappée) n'a volontairement pas été rebranchée séparément (voir `forge`'s `UserController::data()`, qui n'utilise que la forme wrappée, jugée plus idiomatique côté Laravel) : reste non testée en conditions réelles.
- [x] Changer de page, de tri (colonnes Nom/Email/Statut) et de filtre (par nom) : chaque action déclenche bien un rechargement avec les bons résultats. Validé fonctionnellement dans `forge`. — Le débounce 300ms du filtre et le fait qu'une seule requête réseau parte par action n'ont pas été vérifiés précisément (pas d'inspection de l'onglet réseau).
- [x] Vérifier l'état `isLoading` pendant le fetch (skeleton rows). Testé le 2026-09-09 sur `/users` avec une requête réseau interceptée et retardée (Playwright `page.route`) pour rendre le fetch observable : les lignes squelette (`PrSkeleton`) s'affichent bien pendant que la requête est en vol.
- [x] Vérifier que la sélection de lignes (`selectedRows`) se comporte correctement quand on change de page côté serveur. Testé le 2026-09-09 via une page de test dédiée (`/dev/selectable-datatable-test`, `selectable` ajouté sur une instance séparée, `/users` lui-même non modifié) : sélectionner une ligne puis changer de page réinitialise proprement la sélection (pas d'id obsolète, pas d'état incohérent) — comportement déjà correct du `watch(sourceRows, ...)` existant. **Bug majeur trouvé et corrigé au passage** : `PrCheckbox`/`PrSwitch`/`PrToggle` étaient en réalité totalement non-contrôlables — voir `BACKLOG.md`.
- [x] Réduire `totalRows` côté serveur pendant qu'on est sur une page qui n'existe plus (ex: suppression en masse) : vérifier que le composant retombe bien sur la dernière page valide et émet `update:page`. Testé le 2026-09-09 dans `forge` (`/users`, 51 lignes) avec un navigateur Playwright réel : navigation sur la page 6/6 (1 seule ligne), suppression de cette ligne via `PrAlertDialog`. Le composant refait deux requêtes (`page=6` puis `page=5`, cf. `watch(pageCount, ...)` dans `PrDataTable.vue`) et retombe correctement sur "Page 5 sur 5" avec ses 10 lignes, sans erreur console ni état incohérent. Le double aller-retour réseau (page 6 obsolète puis page 5 clampée) est un détail d'implémentation, pas un bug — déjà géré par le `watch(pageCount, ...)` existant du composant.

## usePrForm

- [x] Formulaire avec un champ tableau dynamique (ajout/suppression de lignes) : vérifier que `isDirty` ne reste plus bloqué à `true` en permanence, et que `reset()` restaure bien l'état initial sans le corrompre après plusieurs cycles. Testé le 2026-09-09 via une page de test dédiée dans `forge` (`/dev/dynamic-array-form-test`, champ `contacts: {name, email}[]`), navigateur Playwright réel : ajout de 3 lignes vides (`isDirty` passe à `true`), remplissage, suppression de la ligne du milieu, `reset()` → retour exact à `[]` et `isDirty: false`. Répété sur un deuxième cycle indépendant (ajout/remplissage/suppression/reset) sans dérive, puis vérifié qu'un champ scalaire (`teamName`) modifié après un `reset()` refait bien passer `isDirty` à `true`. Aucune corruption observée — cohérent avec le test unitaire déjà existant (`usePrForm.test.ts`, "reset() restores the field without corrupting the schema initial value", qui couvre déjà exactement ce cas via `structuredClone`).
- [x] Simuler une réponse 422 Laravel avec des clés imbriquées (`items.0.name`, `items.1.email`) sur un schéma qui déclare `items.name`. Testé le 2026-09-09 : `/dev/dynamic-array-form-test` a désormais un vrai bouton "Simuler 422 imbrique" qui déclenche un `ValidationException::withMessages(['contacts.0.name' => '...'])` réel côté Laravel — le message est bien routé vers le champ `contacts.name` du schéma.
- [x] Simuler une réponse 422 avec une clé qui ne correspond à aucun champ du schéma : vérifier que le message apparaît dans `generalErrors`. Testé le 2026-09-09 avec un vrai 422 Laravel sur une clé `quota` non déclarée dans le schéma : le message apparaît bien dans `generalErrors`, affiché via `PrAlert`.
- [x] `isSubmitting` : ajouté le 2026-09-09 à `usePrForm` (`Ref<boolean>`, `true` pendant l'exécution du callback de `handleSubmit`, y compris en cas d'échec de validation avant l'appel — reste `false` puisque le callback n'est jamais lancé). Testé unitairement (`usePrForm.test.ts`). `forge` utilisait jusqu'ici un contournement (`ref` local passé au prop `loading` de `PrButton`) — à migrer vers `form.isSubmitting` directement si l'app est retouchée.

## PrDialog / PrAlertDialog / PrToast (nouveau, testé le 2026-09-09 via `forge`)

- [x] `PrSelect` (ou tout popover) à l'intérieur d'une `PrDialog` : le menu déroulant s'ouvre bien au-dessus de la modale et reste cliquable. Cassé avant le fix z-index de cette session (cf. `BACKLOG.md`), validé après sur mobile (360px) et desktop.
- [x] `PrDialog` utilisée comme modale d'édition (formulaire pré-rempli, boutons dans le slot `footer`, fermeture via la croix/Annuler/Escape) : fonctionne correctement, y compris en responsive.
- [x] `PrAlertDialog` pour une confirmation de suppression : attention, ses boutons `confirm`/`cancel` ferment la modale eux-mêmes (`AlertDialogAction`/`AlertDialogCancel` de Reka UI) en émettant `update:open(false)` **en même temps** que `@confirm`/`@cancel` — un consommateur qui vide sa donnée "cible" dans le handler `update:open` va la perdre avant que `@confirm` ne s'exécute (bug reproduit et corrigé côté `forge` : séparer l'état visuel de la modale de la donnée métier, ne vider cette dernière qu'après l'action réellement effectuée).
- [x] `PrToast` avec `variant` (`success`/`danger`) et barre de progression : rendu correct, couleurs conformes.
- [x] `PrToast` (`duration` par défaut) créé pendant qu'une `PrDialog` est encore ouverte, juste avant sa fermeture : disparaît bien après `duration`, y compris en séquence réaliste (plusieurs modifications à la suite) et contre le build de production. Un soupçon de bug ici plus tôt dans la session ne s'est pas confirmé — cf. `BACKLOG.md` § "Investigué le 2026-09-09".

## PrCalendar — fuseau horaire

Testé le 2026-09-09 avec un vrai navigateur Chromium (Playwright), contexte avec `timezoneId` simulé et horloge système figée (`page.clock.install`) — pas seulement l'horloge de la machine hôte. Page de test jetable (Vite + petit composant hôte), supprimée après coup ; le code de `PrCalendar` (`src/components/molecules/Calendar/utils.ts`) construit et lit toujours des dates en local (`getFullYear`/`getMonth`/`getDate`, jamais `toISOString()` ni `new Date(string)`), donc déjà couvert unitairement dans `utils.test.ts` — ce test navigateur confirme que le composant lui-même (pas seulement les utils) ne réintroduit pas de conversion UTC entre le clic et la valeur émise.

- [x] Sélectionner une date en fin de journée locale (23h+, via un environnement système en `Europe/Paris` ou toute zone UTC+) et vérifier que la valeur émise correspond bien au jour cliqué (pas de décalage de -1 jour côté backend). Vérifié en `Pacific/Auckland` (UTC+13) et `Europe/Paris` (UTC+2, DST) : clic sur le dernier jour du mois affiché → valeur émise identique au jour cliqué. Vérifié aussi avec l'horloge navigateur figée à 23:45 heure locale d'Auckland le 31 janvier 2026 (sans `modelValue`, donc `new Date()`) : le calendrier affiche bien janvier 2026 et le clic sur le 31 émet `2026-01-31` (pas de bascule vers février côté UTC). Aucun décalage constaté dans les trois cas.
- [x] Ouvrir le calendrier avec une `modelValue` déjà définie (ex: `2026-01-01`) et vérifier que le mois affiché au premier rendu est le bon, sans décalage. Vérifié en `Pacific/Auckland` et `Europe/Paris` : le calendrier affiche "janvier 2026" au montage dans les deux fuseaux, aucun décalage vers décembre 2025.

## Soumission de formulaire native (sans JS, `<form method="POST">` Blade classique)

Testé le 2026-09-09 via une page de test dédiée dans `forge` (`/dev/native-form-test`, route + contrôleur + vue Blade + îlot Vue séparés, aucune interférence avec la démo `/users`) : formulaire `<form method="POST">` **réel**, sans `usePrForm`, sans axios, sans `@submit.prevent`, piloté par un navigateur Playwright réel (remplissage de chaque composant, clic sur le bouton `submit`, lecture du payload JSON reçu côté Laravel via `$request->all()`).

- [x] `PrCheckbox` / `PrSwitch` : plusieurs cases avec `name="options[]"` et des `value` distincts → vérifié, Laravel reçoit `options: ["a", "b"]`. `PrSwitch` avec `name="newsletter"` reçu comme `"on"`.
- [x] `PrTagInput` avec `name="tags[]"` : ajout de deux tags via l'UI (saisie + Entrée), Laravel reçoit `tags: ["benevolat", "logistique"]` (pas le texte de saisie en cours, uniquement les tags validés).
- [x] `PrCombobox` en mode simple et en mode `multiple`, avec `name` renseigné : simple → `country: "fr"` ; multiple → `skills: ["accueil", "communication"]`. Les deux modes utilisent des inputs cachés gérés par Prisme lui-même (pas par reka-ui), avec le même `name` répété par valeur pour le mode multiple. **Bug d'affichage trouvé et corrigé le 2026-09-09** (données soumises déjà correctes, seul le rendu était faux — trouvé par l'utilisateur en testant `/dev/native-form-test` à la main) : `display-value` était posé sur `ComboboxRoot` au lieu de `ComboboxInput` (seul endroit où reka-ui le lit), donc le mode simple affichait la valeur brute (`"fr"`) plutôt que le libellé (`"France"`), et le mode `multiple` n'affichait aucune sélection après un choix (champ vidé sans aucune puce visible). Corrigé + ajout d'une rangée de puces retirables individuellement pour le mode `multiple`. Voir `BACKLOG.md`.
- [x] `PrToggleGroup` en `type="single"` et `type="multiple"`, avec `name` renseigné : single → `plan: "yearly"` ; multiple → `days: ["mon", "fri"]`. Point notable (pas un bug) : en `type="multiple"`, l'input caché généré par reka-ui indexe le nom (`days[0]`, `days[1]`, recalculé à chaque changement de sélection) plutôt que de répéter `days[]` — au global équivalent côté PHP (tableau séquentiel identique), mais une convention différente de celle de `PrCombobox`/`PrTagInput`/`PrCheckbox` qui répètent le même `name`. À garder en tête si un jour on inspecte le payload brut (hors `$request->input()`).
- [x] `PrDatePicker` avec `name` renseigné : date sélectionnée (`2026-12-25`) reçue telle quelle côté serveur.
- [x] `PrInput` / `PrNumberInput` / `PrTextarea` : `autocomplete`, `maxlength`, `pattern`, `inputmode` et `data-*` custom vérifiés via l'inspecteur DOM (évaluation JS dans la page) — tous atterrissent bien sur l'élément natif (`<input>`/`<textarea>`), pas sur le `<div>` englobant. Bonus : le `pattern="[a-z]+"` a effectivement bloqué la soumission native du navigateur sur une valeur non conforme (`bénévole`), confirmant que la validation HTML5 fonctionne de bout en bout. `PrTagInput` non re-testé spécifiquement sur ce point (pas d'attribut custom passé dans ce test), mais son `<input>` de saisie a `v-bind="$attrs"` donc le même mécanisme s'applique.
- [x] Bug trouvé et corrigé au passage (sans lien direct avec la checklist, découvert en préparant ce test) : `PrTagInput` et `PrNumberInput` existaient et étaient buildés (`@oremis/prisme/components/atoms/TagInput`, `.../NumberInput`) mais étaient absents de `src/components/registry.ts` — invisibles à `app.use(Prisme)` (enregistrement global) et à `componentPaths`/`paths.generated.ts`. Ajoutés au registre. Voir `BACKLOG.md`.

Page de test conservée dans `forge` (`resources/views/dev/native-form-test.blade.php`, `resources/js/pages/native-form-test.js` + `native-form-test/Index.vue`, route `dev.native-form-test`) — pas commitée, utile comme page de régression manuelle pour un futur upgrade de Prisme. À supprimer si jugée inutile à terme.

## Accessibilité (lecteur d'écran)

Testé le 2026-09-09 via une page de démo dédiée dans `forge` (`/dev/a11y-test`), navigateur Playwright réel (arbre d'accessibilité du DOM — `role`, `aria-label` — plutôt qu'un lecteur d'écran physique, qui n'est pas disponible dans cet environnement ; ces attributs sont exactement ce qu'un lecteur d'écran lit).

- [x] `PrAlert` en variante `danger`/`warning` : vérifier qu'un lecteur d'écran (NVDA/VoiceOver) interrompt et annonce immédiatement le message (role="alert"). Déjà correct dans le code (`PrAlert.vue`, `alertRole` computed) : `role="alert"` confirmé sur les deux variantes.
- [x] `PrSlider` avec `label` : cliquer sur le label et vérifier que le focus va bien sur le curseur ; vérifier que le lecteur d'écran annonce le label fourni (pas "Valeur" en dur) quand on focus le curseur. **Bug trouvé et corrigé** : l'`id` généré était posé sur `SliderRoot` (un conteneur non focusable), pas sur `SliderThumb` (le `<span role="slider" tabindex="0">` réellement interactif) — un clic sur le `<label for="...">` ne focalisait donc rien. `SliderThumb` de reka-ui n'étant pas un élément "labelable" au sens HTML (ni input/button/select/textarea), même avec le bon `id` le comportement natif du label ne suffit pas : ajouté un handler `@click` explicite sur `PrLabel` qui appelle `.focus()` sur le curseur. Vérifié après fix : le clic sur le label envoie bien le focus sur le curseur. L'annonce du label custom (`aria-label`) était déjà correcte (pas de bug là-dessus).
- [x] **Bug distinct trouvé le 2026-09-09** (par l'utilisateur, en testant `/dev/a11y-test` à la main) : `PrSlider` utilisé sans `v-model` (seulement `default-value`, exactement le cas de la démo a11y) ne pouvait pas du tout être déplacé — le curseur revenait à sa position de départ dès qu'on le glissait. Cause : `sliderValue` était calculé comme `modelValue ?? defaultValue` et bindé tel quel sur `SliderRoot`, qui restait donc en permanence "contrôlé" sur une valeur constante (rien ne fait jamais évoluer `defaultValue`). Corrigé en gardant un état interne qui suit le drag en l'absence de `v-model`, tout en laissant `modelValue` prioritaire dès qu'il est fourni. Voir `BACKLOG.md`.
- [x] `PrThemeToggle` avec une prop `label` custom : vérifier que le lecteur d'écran annonce ce label plutôt que le message dynamique par défaut. Déjà correct dans le code (`accessibleLabel = props.label ?? nextThemeLabel.value`) : vérifié, le `aria-label` est exactement le label custom fourni quand il est présent.

## Responsive (testé le 2026-09-09 via `forge`, Chromium/Playwright headless)

- [x] Pas de scroll horizontal de page à 360px, 390px, 780px et 1440px de large sur une page avec `PrDataTable` + formulaire complexe (`PrToggleGroup` 7 items, `PrSelect`) + `PrDialog`. Un bug d'app consommatrice (wrapper `display:grid` sans colonnes explicites, cf. `BACKLOG.md`) faisait déborder la page et sortir `PrNavbar` du cadre au scroll horizontal — corrigé côté `forge`, pas un bug Prisme, mais bon réflexe à vérifier sur toute nouvelle page.
- [x] `PrSidebar` en dessous de 780px : passe bien en rail icônes réduit (`data-collapsed`/`max-[780px]`), le bouton `PrSidebarCollapseButton` fonctionne.
- [x] `PrDataTable` sur petit écran : les colonnes en trop restent accessibles via le scroll horizontal interne (`.pr-data-table__scroll`) sans faire déborder la page.
- [x] Tiroir mobile `PrSidebar` en overlay (`data-mobile-expanded`) : testé le 2026-09-09 dans `forge` (n'importe quelle page, le sidebar est partagé par le layout), navigateur Playwright réel à 375px de large. **Bug trouvé et corrigé** : il n'y avait ni scrim/overlay assombrissant le contenu derrière le tiroir déployé, ni fermeture au clic en dehors — seule la fermeture au clic sur un item existait (`closeMobileFlyout()` déjà appelé par `PrSidebarItem`). Ajouté dans `PrSidebar.vue` : un `<div>` de backdrop (`bg-[var(--pr-color-overlay)]`, même token que `PrDialog`/`PrSheet`) affiché sous le tiroir ouvert et au-dessus du contenu, qui ferme le tiroir au clic ; ajouté aussi la fermeture au clavier (`Escape`). Vérifié après fix : le backdrop apparaît à l'ouverture, le clic dessus referme le tiroir.

## Thème / Navbar

- [x] Dans une app consommatrice, redéfinir `--pr-color-navbar`, `--pr-color-navbar-text` et `--pr-color-navbar-muted` et vérifier que `PrNavbar` reflète bien ces valeurs en thème clair **et** sombre. Testé le 2026-09-09 via `/dev/navbar-theme-test`. **Bug de spécificité CSS trouvé et corrigé** : `themes.css` définissait les tokens sur `:root[data-pr-theme="light"]`/`:root[data-pr-theme="dark"]` (spécificité 0,2,0) en plus de la forme simple `[data-pr-theme="x"]` (0,1,0) — un override consommateur naïf en `:root { --pr-color-navbar: ... }` (0,1,0, exactement le pattern documenté dans ce README) perdait donc systématiquement, en clair comme en sombre. Corrigé en retirant la variante composée redondante (`:root[data-pr-theme="x"]` n'apporte rien que `[data-pr-theme="x"]` seul ne couvre déjà, `:root` **étant** `<html>`). Vérifié en **build de production** (`rgb(124, 58, 237)` bien appliqué) — en mode dev Vite, l'injection asynchrone du CSS importé depuis `app.js` le place après le `<style>` de la page, ce qui fait perdre l'égalité de spécificité au tie-break par ordre ; artefact propre au dev server, sans impact en production où `@vite()` émet le lien CSS de Prisme avant `@stack('styles')`.
- [x] Injecter `getPrThemeInitScript()` dans le `<head>` Blade et faire un hard-reload avec le thème sombre choisi : vérifier qu'il n'y a aucun flash blanc avant que la page s'affiche en sombre. Déjà en place dans `forge` (`@include('partials.prisme-theme-init')` avant `@vite(...)`). Vérifié le 2026-09-09 : `document.documentElement.dataset.prTheme` vaut `dark` dès `DOMContentLoaded` après un rechargement avec le thème sombre stocké.

## Navigation

- [x] `PrSidebarItem` / `PrSidebarSubItem` / `PrNavigationMenu` sans prop `active` explicite, sur des routes Blade réelles avec sous-pages. Testé le 2026-09-09 via `/dev/settings-test` + `/dev/settings-test/security` : le lien parent reste actif sur la sous-route, sans prop `active` explicite.
- [x] Vérifier qu'un `href` qui est un préfixe accidentel d'un autre (ex: `/settings` vs `/settings-other`) ne déclenche pas un faux positif. Testé le 2026-09-09 via `/dev/settings-test-other` : le lien parent (`/dev/settings-test`) ne s'active pas faussement sur cette route sœur au préfixe proche.
- [x] Piège d'intégration (pas un bug Prisme) : `PrSidebarItem` compare `href` à `window.location.pathname`, qui est toujours un **chemin relatif**. Si `href` est généré via `route('...')` côté Laravel (URL absolue par défaut), l'item n'est jamais actif. Trouvé et corrigé dans `forge` en passant `route('users.index', absolute: false)`.

## Pagination

- [x] `PrPagination` avec un `pageCount` élevé (ex: 500) : vérifier que la troncature `1 … 49 50 51 … 500` s'affiche et se met à jour correctement en naviguant vers les extrémités. Testé le 2026-09-09 via une page de démo dédiée dans `forge` (`/dev/pagination-test`, `pageCount=500`), navigateur Playwright réel : à la page 50, affiche exactement `1 … 49 50 51 … 500` ; à la dernière page (500), affiche `1 … 499 500` (pas de fenêtre après la dernière page, correct). Aucun bug — déjà couvert par `buildPaginationItems` et ses tests unitaires existants (`utils.test.ts`).
- [x] `PrPagination` avec `totalRows`/`pageSize` (nouvelle API, sans calculer `pageCount` à la main) : testé le 2026-09-09 via `/dev/pagination-total-rows-test`, branché directement sur `fromLaravelPaginator`. `pageCount` correctement dérivé (`Math.ceil(51/10)=6`), navigation fonctionnelle.

## FileUpload

- [x] `PrFileUpload` avec upload réel (`upload` prop, axios + progress + erreur 422 par fichier). Testé le 2026-09-09 via `/dev/file-upload-test` avec un vrai endpoint Laravel : succès sans erreur, échec 422 affiché sous le fichier concerné (pas un `error` global), bouton "Réessayer" fonctionnel.
- [x] Déposer plus de fichiers que la limite `maxFiles` restante (ex: `maxFiles=3`, 2 déjà présents, dépôt de 5 fichiers d'un coup) : vérifier qu'exactement 1 fichier est accepté et 4 rejetés. Testé le 2026-09-09 via `/dev/file-upload-test` : 3 acceptés au total (2+1), 4 rejetés sur le second dépôt de 5 — comportement correct.

## RichTextEditor

Extrait le 2026-09-12 de `LessonEditor.vue` (Formation) vers `PrRichTextEditor`. La suite automatisée (`PrRichTextEditor.test.ts`, jsdom + `@vue/test-utils`, pilotage de l'instance tiptap via `wrapper.vm.editor`/`insertImageFromFile` exposés) et la story `Interaction` (Storybook + Playwright, frappe réelle dans le contenteditable) couvrent la logique et le rendu en navigateur réel — reste à valider manuellement dans une app consommatrice (Formation) :

- [ ] Soumission de formulaire native (`<form method="POST">` sans JS) avec un `name` renseigné sur `PrRichTextEditor` : vérifier que le serveur reçoit bien le HTML courant via l'`<input type="hidden">` généré, y compris après plusieurs modifications successives (pas de valeur figée à l'état initial).
- [ ] Upload d'image en conditions réelles avec un vrai endpoint (`uploadImage` branché sur un contrôleur Laravel) : succès (image insérée avec la bonne URL), échec réseau/422 (message d'erreur affiché, aucune image insérée), et upload concurrent de plusieurs images à la suite.
- [ ] Édition d'une leçon existante : le contenu initial (`modelValue`) s'affiche bien au montage, round-trip complet sans perte de mise en forme (gras, callouts, tableaux, listes à cocher, sections repliables) après plusieurs cycles de sauvegarde/rechargement.
- [ ] Accessibilité clavier de la toolbar : navigation au clavier (Tab) à travers tous les boutons, activation au clavier (Entrée/Espace), focus visible (`focus-visible`), et lecture correcte des `aria-label` par un lecteur d'écran (NVDA/VoiceOver) — en particulier le bouton couleur (`<input type="color">` superposé) et le picker d'emoji (`role="menu"`/`role="menuitem"`).
- [ ] Chaque variante `Callout` (info/succès/avertissement/danger) : insertion, bascule vers une autre variante sans dupliquer l'encadré, et retrait (toggle sur la même variante) qui "délift" le contenu sans le perdre. Vérifier aussi que `editor-content.css` rend les callouts à l'identique en édition et dans la vue étudiant en lecture seule (couleurs, icône, retrait `border-left`).
- [ ] Comparaison visuelle du rendu HTML avant/après migration entre l'ancien `_lesson_content.scss` (Bootstrap) et `@oremis/prisme/styles/editor-content.css` (tokens `--pr-*`) sur un contenu de leçon existant représentatif (tableaux, code, callouts, détails) — les tokens de couleur ont changé de valeurs exactes (palette Prisme vs Bootstrap), un écart de nuance est attendu, seule une régression de lisibilité/contraste serait un bug.
- [ ] Prop `disabled` : vérifier que l'éditeur devient réellement non éditable (`editor.isEditable === false`), que tous les boutons de la toolbar sont désactivés, et qu'aucune image ne peut être déposée (drag & drop) pendant que l'éditeur est désactivé.
