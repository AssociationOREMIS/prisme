# Backlog

Ce qui reste à faire ou à décider sur Prisme. Voir `TESTING.md` pour la checklist de validation manuelle.

## Rappel : l'enregistrement dans `registry.ts` est manuel et facile à oublier

`PrTagInput`/`PrNumberInput` avaient déjà été oubliés une fois (voir plus bas, § "Soumission de formulaire native") : buildés et exportables via `@oremis/prisme/components/...`, mais absents de `src/components/registry.ts`, donc invisibles à `app.use(Prisme)` et à `componentPaths`/`paths.generated.ts`. `PrRichTextEditor` (ajouté le 2026-09-12) a bien été enregistré dans `registry.ts` (+ `src/index.ts`) dès sa création — mais comme ce n'est vérifié par aucun test automatisé (rien n'échoue si un nouveau composant reste juste un dossier sous `src/components/`), tout futur nouveau composant reste exposé au même oubli. À envisager : un test qui compare la liste des dossiers sous `src/components/{atoms,molecules,layouts}` à `Object.keys(componentRegistry)` pour détecter l'oubli automatiquement.

## Bug critique corrigé le 2026-09-09 (noté a posteriori vu la gravité)

**`PrCheckbox`, `PrSwitch` et `PrToggle` étaient totalement non-contrôlables** : leur wiring interne passait `:checked`/`:default-checked` (ou `:pressed`/`:default-pressed`) aux composants reka-ui sous-jacents (`CheckboxRoot`, `SwitchRoot`, `Toggle`), mais ces trois primitives reka-ui utilisent en réalité `modelValue`/`update:modelValue` (et `defaultValue`), pas `checked`/`pressed`. Résultat : `:checked`/`@update:checked` atterrissaient dans les `$attrs` de la primitive (silencieusement absorbés, aucune erreur), qui tournait alors en mode **non contrôlé** — un clic faisait bien visuellement basculer l'état (`data-state`), mais aucun `update:checked`/`update:pressed` n'était jamais émis vers le parent, et changer la prop `checked` depuis le parent n'avait aucun effet visuel. Un `v-model:checked`/`v-model:pressed` — le pattern documenté dans le README — ne fonctionnait donc jamais réellement.

Trouvé en branchant `PrCheckbox` dans un vrai scénario contrôlé (`PrDataTable` `selectable`, page de test `/dev/selectable-datatable-test`) : la sélection ne se répercutait jamais côté parent malgré un clic visuellement "réussi". Confirmé aussi sur `PrSwitch`/`PrToggle` (même schéma reka-ui `modelValue`).

**Corrigé** dans `src/components/atoms/Checkbox/PrCheckbox.vue`, `src/components/atoms/Switch/PrSwitch.vue`, `src/components/atoms/Toggle/PrToggle.vue` : binding changé vers `:model-value`/`:default-value`/`@update:model-value` en interne, l'API publique de ces trois composants (`checked`/`pressed`, `update:checked`/`update:pressed`) reste inchangée. Vérifié : `npm run type-check` + 177 tests passent, et en navigateur réel — un header "select all" de `PrDataTable` (jamais cliqué lui-même) reflète correctement l'état `checked`/`indeterminate` calculé par le parent, et cliquer ce header propage bien `checked` à toutes les lignes (jamais cliquées individuellement) : contrôle externe confirmé dans les deux sens, pas seulement au clic direct.

Ce bug touchait potentiellement **tout usage contrôlé** de ces trois composants dans `forge` ou tout futur projet — à surveiller si un comportement de case à cocher/switch/toggle semblait "figé" avant cette date.

## `PrScrollArea` : le contenu débordant était coupé sans scroll possible

Trouvé le 2026-09-09 par l'utilisateur sur `/dev/components-smoke-test` ("semble pas fonctionner"). `ScrollAreaRoot` ne reçoit qu'un `max-height` (il grandit avec le contenu jusqu'à ce plafond, ce n'est pas une hauteur fixe) — `ScrollAreaViewport` était en `height: 100%`, un pourcentage de hauteur dans un parent dont la hauteur reste "auto" ne fait rien en CSS. Le viewport ne se limitait donc jamais vraiment, et une fois le contenu plus haut que `max-height`, c'est l'`overflow-hidden` du conteneur externe qui coupait la fin du contenu — sans aucun moyen de scroller pour la voir.

Corrigé (`src/components/molecules/ScrollArea/PrScrollArea.vue`) avec le pattern déjà utilisé par `PrDialog` pour le même problème : `flex flex-col` sur `ScrollAreaRoot`, `flex-1 min-h-0` sur `ScrollAreaViewport` (au lieu de `size-full`), et les scrollbars repositionnées en overlay `absolute` plutôt qu'en flux normal.

## `PrSidebar` : badges de notification + réalignement visuel sur `data`/`formation` (2026-09-13)

Ajouté `badge-count`/`badge-max`/`badge-dot` sur `PrSidebarItem` et `PrSidebarSubItem` (rendu par `PrSidebarIcon`) : pastille rouge ancrée sur l'icône (visible en sidebar réduite comme étendue), avec ou sans chiffre (plafonné à `badge-max`, `99+` par défaut au-delà). Demande initiale : reproduire le compteur de `pending-changes` de `data` (`resources/views/partials/sidebar.blade.php`, alimenté par un `View::composer` dans `AppServiceProvider.php`), mais en générique/réutilisable plutôt que couplé à une route précise.

En creusant `data`/`formation` (`resources/sass/_base.scss`) pour ce ticket, l'utilisateur a demandé au passage de rapprocher visuellement `PrSidebar` de ces deux apps ("je trouve celle de data et formation colle plus à nos designs actuels"). Comparaison des valeurs hex codées en dur dans `_base.scss` avec `src/styles/tokens.css` : correspondances exactes déjà en place (`--sidebar-icon-bg: #f1f5f9` = `--pr-neutral-100`, `--sidebar-border: #e2e8f0` = `--pr-neutral-200`, `--navbar-bg: #0d2c99` = `--pr-blue-900`), mais trois écarts réels :
- Le fond de la sidebar (`--sidebar-bg: #f8fafc` = `--pr-neutral-100`... en fait `--pr-color-background`, pas `--pr-color-surface` qu'utilisait `PrSidebar.vue` — la sidebar Prisme était blanc pur, `data`/`formation` légèrement gris).
- La boîte d'icône avait une bordure (`border-[var(--pr-color-border)]`) et un survol teinté bleu (`primary-soft`/`primary-border`/`primary`) ; `data`/`formation` n'ont aucune bordure sur `.nav-icon` et un survol neutre (gris plus foncé, jamais de bleu en aperçu avant l'état actif).
- L'icône active passait en bleu (`--pr-color-primary`, cohérent avec la navbar) ; `data`/`formation` utilisent un vert dédié (`--sidebar-active-bg: #31c48d`, à l'origine une teinte Flowbite) pour contraster avec la navbar bleu foncé. Mappé d'abord sur `--pr-color-success` (token sémantique le plus proche) — **puis sur demande explicite ("utilise au le même vert que data stp") remplacé par un token dédié `--pr-color-sidebar-active: #31c48d`** (`src/styles/themes.css`, défini identique en clair/sombre faute d'équivalent sombre chez `data`/`formation`) : `--pr-color-success` reste inchangé pour tout le reste de la librairie (badges, toasts...), seul l'accent actif de la sidebar utilise la teinte exacte de `data`/`formation`.

Corrigé dans `sidebarIconStyles.ts` (bordure supprimée, survol et état actif recolorés) et `PrSidebar.vue` (fond `--pr-color-background`). Vérifié visuellement (Playwright, `Complete example` story) en clair, en sombre, au survol et avec les nouveaux badges — pas de régression sur les 198 tests. Le bouton "Réduire la navigation" (`PrSidebarCollapseButton`) garde volontairement son fond `--pr-color-surface` (blanc) : il doit rester visible comme un bouton posé sur le nouveau fond gris de la sidebar, pas se fondre dedans.

**Deuxième passe (même jour)** : la simple correspondance de couleurs ne suffisait pas ("tu peux utiliser vraiment le même design... c'est pas possible ?") — l'espacement/la densité restaient ceux, plus spacieux, d'origine Prisme. Repris `_base.scss` pour les métriques exactes et mappé sur les tokens Prisme correspondants :
- `.nav-item-card` : `padding: 0` + `gap: 1rem` (16px) → `sidebarItemLinkBaseClass` passe de `p-[var(--pr-space-2)]`/`gap-[var(--pr-space-3)]`/`min-h-[3.25rem]` à `p-0`/`gap-[var(--pr-space-4)]`, hauteur de ligne désormais purement intrinsèque (l'icône de 36px la contraint déjà).
- `.nav-icon` : `border-radius: 0.5rem` (8px, = `--pr-radius-lg`) alors que l'icône Prisme était en `--pr-radius-md` (6px).
- `.nav-title` : `font-size: 0.9375rem` = `--pr-font-size-md` (Prisme utilisait `--pr-font-size-sm`, 14px au lieu de 15px). `.nav-subtitle` : `0.8125rem` (13px, pas de token exact → valeur arbitraire), couleur alignée sur `--pr-color-text-subtle` (= `--pr-neutral-400`, la vraie correspondance hex du `--sidebar-text-muted` de `data`/`formation`, pas `--pr-color-text-muted`/neutral-500 utilisé jusque-là).
- `.sidebar-nav { padding: 1.25rem 1rem }` → le conteneur scroll passe de `px-[var(--pr-space-3)] pt-5 pb-4` (asymétrique, 12px horizontal) à `px-[var(--pr-space-4)] py-[var(--pr-space-5)]` (16px/20px symétrique).
- Les overrides de padding collapsed/mobile dans `PrSidebar.vue` (qui restauraient l'ancien `p-2` de base) nettoyés en cohérence (`py-0` en collapsed, comme `data`/`formation` ; plus de ré-ajout de padding en flyout mobile).

Non repris (périmètre jugé trop profond pour ce ticket, à discuter si besoin) : les sous-items de `.nav-submenu` chez `data`/`formation` ont une icône plus petite (32px vs 36px) et un padding propre (0.5rem/0.75rem) — `PrSidebarItem` imbriqué (utilisé pour les sous-menus) garde la même taille d'icône qu'au premier niveau, et `PrSidebarItemSubItems` garde son propre style Prisme (ligne verticale de guidage `border-l`) plutôt que le simple retrait `padding-left: 1.5rem` de `data`/`formation`. Revérifié visuellement (clair/sombre/survol/collapsed/badges) + 198 tests toujours au vert après cette deuxième passe.

**Troisième passe (même jour)** : l'utilisateur a demandé une analyse explicite ("pourquoi pas les mêmes icônes/dimensions/le même réduire la navigation ? dis-moi ce qui t'empêche"). Réponse après vérification :
- Dimensions (`--pr-sidebar-width`/`--pr-sidebar-collapsed-width`/`--pr-navbar-height`) : déjà identiques à `--sidebar-width`/`--sidebar-collapsed-width`/`--navbar-height` (280px/70px/60px) — rien à faire, correspondance déjà exacte.
- Bouton "Réduire la navigation" : aucune contrainte réelle, c'était juste un choix de style par défaut. Refait à l'identique de `.sidebar-close-btn` dans `PrSidebarCollapseButton.vue` : pleine largeur, fond plat (`--pr-color-surface-subtle`), séparateur `border-t` uniquement (plus de pilule blanche bordée/arrondie). `PrSidebar.vue` restructuré en conséquence (le bouton sort du wrapper `footer` paddé/bordé, qui ne sert plus qu'au slot `#footer` custom).
- Icônes : seul vrai point d'architecture (pas un blocage). `data`/`formation` utilisent Font Awesome (classes CSS) ; **tout** Prisme (pas que la sidebar) attend un composant Vue dimensionné via `:size`/`:stroke-width`, l'API de Lucide — la seule bibliothèque d'icônes de la librairie. Faisable via un adaptateur, mais introduirait la première icône non-Lucide de Prisme (rupture de cohérence visuelle avec le reste de la librairie). Laissé en l'état, décision à prendre séparément si besoin.

Au passage, l'utilisateur a noté que l'animation du chevron de sous-menu différait : Prisme utilisait `ChevronDown` (pointant déjà vers le bas, pivotant à 180° = vers le haut une fois ouvert) alors que `data`/`formation` utilisent `fa-chevron-right` (pointant à droite, pivotant à 90° = vers le bas une fois ouvert, `transition: transform 0.2s ease`). Corrigé dans `PrSidebarItem.vue` : `ChevronRight` + `rotate-90`, durée alignée sur 200ms (reste sur l'easing standard de Prisme, différence jugée imperceptible).

Puis l'utilisateur a demandé la teinte de vert exacte plutôt que le token `--pr-color-success` mappé en passe 1 (voir plus haut, remplacé par `--pr-color-sidebar-active: #31c48d`).

**Quatrième passe (même jour)** : trois demandes complémentaires, au-delà de la seule sidebar —
- **Logo navbar** : `PrNavbar.vue` retombait sur `/oremis-logo.svg` (un chemin local que l'app consommatrice doit fournir elle-même). Changé en `https://static.oremis.fr/img/logos/oremis-logo-white.svg` (vérifié accessible, `image/svg+xml`, 19 Ko) — le vrai logo OREMIS s'affiche par défaut sans qu'aucune app n'ait à déposer de fichier local. `logoSrc` reste disponible pour surcharger.
- **Police** : `data`/`formation` chargent `Roboto` (Google Fonts, `app.blade.php`) ; `--pr-font-sans` de Prisme listait `Inter` en premier — mais Prisme ne chargeait déjà aucune police lui-même (`preview-head.html` de Storybook était vide, donc même les captures précédentes de cette session rendaient en fallback système, pas en Inter). Changé pour `Roboto` en tête de pile (même charge que `data`/`formation` : c'est à l'app consommatrice de charger la police, documenté dans `README.md` § Typographie) + ajouté le lien Google Fonts dans `preview-head.html` pour que Storybook reflète le rendu réel.
- **Fond gris de l'app** : `.main-wrapper` de `data`/`formation` est en `#f1f5f9` (= `--pr-neutral-100`/`--pr-color-surface-subtle`), *plus gris* que leur sidebar (`#f8fafc`/neutral-50, déjà traité passe 1). `PrAppShell.vue` utilisait `--pr-color-background` (neutral-50) pour tout le shell, donc sidebar et contenu avaient la même teinte — changé le fond racine du shell en `--pr-color-surface-subtle` pour retrouver la même hiérarchie à deux teintes (contenu légèrement plus gris que la sidebar).

Revérifié visuellement (Playwright) + 198 tests toujours au vert après ces deux dernières passes.

**Cinquième passe (même jour)** : titre de démo illisible + nouveau composant `PrNavbarUserMenu`.
- **`.nav-story-title` blanc sur la page Docs** : signalé par l'utilisateur ("non lisible car blanc"). Reproduit uniquement en vue Docs (autodocs), pas en vue Story isolée — `.nav-story-title` (le `<h1>` "Contenu applicatif" des démos) n'avait pas de couleur explicite et héritait normalement de `--pr-color-text`, mais la page Docs (thème sombre du manager Storybook) impose sa propre couleur de titre par défaut qui gagnait par héritage direct dans le même document. Corrigé en ajoutant `color: var(--pr-color-text)` explicite à `.nav-story-title` (`navigation.stories.css`) — un bug de story de démo, pas de `PrSidebar`/`PrAppShell` eux-mêmes.
- **`PrNavbarUserMenu`** (nouveau, `src/components/layouts/Navbar/PrNavbarUserMenu.vue`) : demande explicite de reproduire "le même système déroulant que Data" pour le menu utilisateur (nom, identifiant entre parenthèses, dropdown). Construit sur `PrDropdownMenu` existant plutôt qu'en composant monolithique : trigger (icône `CircleUser` + nom + `identifier` optionnel entre parenthèses + chevron qui pivote à l'ouverture), en-tête du menu généré depuis les props (`name`/`identifier`/`email`/`roles` joints par " | ", `rolesFallback`), puis un slot par défaut (mêmes render-props que `PrDropdownMenu` : `item`/`separator`/`itemClass`/`dangerItemClass`/`separatorClass`) pour que l'app consommatrice ajoute ses propres entrées ("Mes demandes", "Déconnexion" en rouge...), exactement la structure du `<ul class="dropdown-menu">` de `data`/`formation` mais en Vue/Reka UI plutôt qu'en Bootstrap. Enregistré dans `registry.ts`/`src/index.ts`/`Navbar/index.ts` (cf. rappel en tête de ce fichier sur l'oubli facile de cette étape). Stories ajoutées (`Default`, `WithExtraItem`, `WithoutIdentifier`).
- **Bug trouvé au passage (préexistant, pas lié à `PrNavbarUserMenu`)** : dans `PrDropdownMenu.vue`, `dangerItemClass` concatenait `itemClass` (qui contient déjà `text-[color:var(--pr-color-text)]`) puis `text-[color:var(--pr-color-danger)]` — même propriété CSS, même spécificité : c'est l'ordre d'insertion dans la feuille de style généré (pas l'ordre des classes dans le HTML) qui décidait, et le texte "normal" gagnait silencieusement. Concrètement, tout item "danger" de `PrDropdownMenu` dans toute la librairie (y compris les actions de ligne `PrDataTable`) s'affichait en texte normal au lieu de rouge. Corrigé avec `!` (déjà le pattern maison pour ce genre de conflit, vu dans `PrRichTextEditor`) : `text-[color:var(--pr-color-danger)]!`. Vérifié (couleur calculée `rgb(220, 38, 38)` après coup, contre `rgb(15, 23, 42)` avant).

Vérifié visuellement (Playwright) + type-check + 201 tests (198 + 3 nouvelles stories `PrNavbarUserMenu`) au vert.

## Couverture de tests d'interaction

Le pattern `play` function (Storybook + `storybook/test`) a été introduit sur `PrDialog`, `PrSheet`, `PrTabs`, `PrCarousel` et `PrRichTextEditor` (`Interaction` story, 2026-09-12 : frappe réelle dans le contenteditable + clic toolbar, exécutée en navigateur réel via Playwright) — jusqu'ici aucune story n'en avait, la suite ne faisait que vérifier l'absence d'erreur au rendu. Reste à étendre le même pattern aux autres composants interactifs qui n'en ont pas encore (`PrAccordion`, `PrCombobox`, `PrSelect`, `PrPopover`, `PrDropdownMenu`, `PrToggleGroup`, `PrCommand`, etc.), au fil de l'eau plutôt qu'en un seul lot.
